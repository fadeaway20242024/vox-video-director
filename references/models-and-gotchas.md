# Models & Gotchas — no-Atlas fork

Read this before debugging the no-Atlas pipeline.

## Default services

| Job | Service | Why |
|---|---|---|
| Keyframe / collage poster | Codex `imagegen` | high-quality posters generated by the host agent |
| Public image hosting | imgw.cc | Agnes requires public HTTPS image URLs |
| Image-to-video | `agnes-video-v2.0` | default no-Atlas video backend |
| Narration | local file | no remote audio API in this fork |
| Music / SFX | local file | no remote audio API in this fork |

## Agnes / imgw.cc gotchas

1. Agnes image-to-video must receive a public HTTPS image URL. Local `keyframe_path` values are
   uploaded through imgw.cc by `scripts/agnes.py` when `IMGW_API_KEY` is set.
2. `.env` is auto-loaded from the current project tree. `AGNES_API_KEY` is preferred; `VIDEO_API_KEY`
   is accepted as a compatibility alias.
3. imgw.cc defaults: `IMGW_API_URL=https://www.imgw.cc/api/1/upload`,
   `IMGW_SOURCE_FIELD=source`, `IMGW_AUTH_HEADER=X-API-Key`.
4. Agnes frame counts are normalized to `8n+1` and capped at 441 frames.
5. POST generation calls are not retried before submission; polling resubmits only after failure/stall
   through `provider.run_jobs()`.

## ffmpeg gotchas (this machine's build)

The local ffmpeg is minimal: **no libass, no drawtext** (no libfreetype). So:
- **Captions & watermark → render PNGs with Pillow and composite via `overlay`**
  (`scripts/text_overlay.py`). Size text by `min(W,H)` so it works in 16:9 and 9:16.
- **Off-aspect clips** (e.g. a 3:4 card in a 9:16 frame): use a blurred-cover background +
  fitted sharp foreground via `overlay`, not black bars.
- **Slow, don't freeze**: if a clip is shorter than its narration-driven segment, stretch it
  with `setpts` to fill instead of `tpad` freezing the last frame.
- **`blend` needs both inputs in the same pixel format** — convert both to `gbrp` before a
  `screen` blend (confetti overlay) or the whole frame turns magenta.
- **`sidechaincompress` output length follows its sidechain** — `apad` the narration track to
  the full duration, or `-shortest` will cut a music-only ending (e.g. a scrapbook outro).
- **Whip transitions**: `xfade` slides between shots; recompute each beat's start time
  (each transition overlaps by the transition duration) so narration/captions stay in sync.
- **Verify by extracting frames** — you can't read an mp4's content. `mjpeg` refuses
  full-range YUV, so add `format=yuvj420p` when grabbing jpgs.

## Cost

Depends on Agnes usage, imgw.cc hosting and whichever local/third-party audio workflow you choose.
